{
  "_comment": "Suggested Claude Code permission allowlist for the quali-claude-plugin. Merge the `permissions.allow` entries into your project's .claude/settings.local.json (or run /torque-quickstart and let the skill do it for you). The `configure --token-stdin` invocation is intentionally NOT included — credential writes should stay human-in-the-loop.",
  "permissions": {
    "allow": [
      "Bash(python *torque_api.py:*)",
      "Bash(python3 *torque_api.py:*)",
      "Bash(python *zero-touch-api/scripts/examples/*)",
      "Bash(python3 *zero-touch-api/scripts/examples/*)"
    ]
  }
}
