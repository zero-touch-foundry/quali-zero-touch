{
  "mcpServers": {
    "TorqueMCP": {
      "type": "http",
      "url": "${TORQUE_MCP_URL:-https://portal.qtorque.io/mcp}",
      "headers": {
        "Authorization": "Bearer ${TORQUE_API_TOKEN}"
      }
    }
  }
}
